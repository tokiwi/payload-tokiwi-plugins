---
'@tokiwi/payload-blocks-main': minor
---

Add the blocks package: the `imageCarousel` schema, the `tokiwiBlocksMain` registry plugin
and the `add` command that copies a Mantine front-end template into a project.
